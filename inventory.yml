all:
  vars:
    ansible_ssh_private_key_file: /root/.ssh/id_rsa
    ansible_user: root
    filebeat_remote_path: "/ansible_playbook_filebeat/roles/filebeat/files/filebeat-8.11.1-x86_64.rpm"
    #log path
    kafka_broker_log_path: /var/log/kafka/server.log
    connect_log_path: /var/log/kafka/connect.log
    zookeeper_log_path: /var/log/kafka/ zookeeper-gc.log

    logstash_ip: 172.31.103.209
    logstash_port: 5044

zookeeper:
  hosts:
    zookeeper1:

kafka_broker:
  hosts:
    kafka1:

server:
  hosts:
    server1:
      ansible_host: 172.31.103.209

#kafka_connect:
 # hosts:
  #  kafka-connect:
